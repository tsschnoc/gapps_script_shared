//var fs = require('fs');

//fs.watch('a.log', {}, function (ev, fn) {
//    console.log('event', ev, fn);
//});

// fs.open(path, flags, [mode], [callback])
// fs.openSync(path, flags, [mode])

// fs.read(fd, buffer, offset, length, position, [callback])


//var tailf = require('tailf');
//
//var watchinglog = new tailf.simpleTailf('a.log');
//
//watchinglog.on('data', function (data) {
//    console.log('Data arrived: ' , data.toString());
//});

var tail = require('tailfd').tail,
watcher = tail('a.log',function(line,tailInfo){
  //default line listener. optional.
  console.log('line of data> ',line);
});
